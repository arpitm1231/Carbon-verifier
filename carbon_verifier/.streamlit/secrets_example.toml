# Ye file Streamlit Community Cloud par "Secrets" section mein paste karni hai
# (App settings -> Secrets). Apne laptop par isko secrets.toml naam se
# is .streamlit folder ke andar bhi rakh sakte ho local testing ke liye.
#
# EE_SERVICE_ACCOUNT_JSON = poori Google Cloud service-account JSON key
# (single line string) — README ke "Hosting" section mein banane ka tarika hai.
#
# EE_PROJECT_ID = aapka Google Cloud project id

EE_SERVICE_ACCOUNT_JSON = '''{"type": "service_account", "project_id": "your-project-id", "private_key_id": "...", "private_key": "...", "client_email": "your-service-account@your-project-id.iam.gserviceaccount.com", "client_id": "...", ...}'''

EE_PROJECT_ID = "your-project-id"
